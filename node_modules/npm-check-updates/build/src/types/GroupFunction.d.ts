import { UpgradeGroup } from '../version-util';
/** Supported function for the --group options. */
export declare type GroupFunction = (packageName: string, currentVersion: string, upgradedVersion: string, defaultGroup: UpgradeGroup) => UpgradeGroup;
